{
  "name": "patman-inventory-dashboard",
  "version": "2.0.0",
  "description": "Patman Inventory Dashboard — Google Apps Script + BigQuery",
  "scripts": {
    "push": "clasp push",
    "pull": "clasp pull",
    "deploy": "clasp deploy --description \"Production deploy $(date +%Y-%m-%d)\"",
    "open": "clasp open",
    "logs": "clasp logs --watch",
    "status": "clasp status"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
